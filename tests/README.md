# Tests

Suggested next tests:

- Auth signup/login token flow.
- Transaction CRUD and filtering.
- Analytics summary from seeded transactions.
- Budget usage calculation.
- Chat fallback behavior when no AI key is configured.
